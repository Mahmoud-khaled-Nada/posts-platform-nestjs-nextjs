"use client"
import { FC } from "react";
import { ChildrenType } from "@/utils/types";

const LoginLayout: FC<ChildrenType> = ({ children }) => {
  return <>{children}</>;
};

export default LoginLayout;
